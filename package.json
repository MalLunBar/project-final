{
  "name": "project-final-parent",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm install --prefix backend"
  },
  "dependencies": {
    "bcrypt-nodejs": "^0.0.3",
    "express-list-endpoints": "^7.1.1",
    "zustand": "^5.0.7"
  }
}
